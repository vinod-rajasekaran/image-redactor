presidio-analyzer
presidio-image-redactor
pytesseract
Pillow
spacy
rich

# Visual PII (on by default): faces, QR and 1-D barcodes. opencv-contrib
# provides cv2.barcode; plain opencv-python lacks it.
opencv-contrib-python

# Optional, --pyzbar only: decodes code payloads. Adds nothing to detection
# and needs the zbar system library (brew install zbar).
pyzbar

# Alternative OCR backend (--ocr paddle). Large (~500MB) and CPU-slow,
# but markedly better on photographed/tilted documents.
paddleocr
paddlepaddle

# Optional, --medical-ner only: clinical entity detection (diagnoses,
# medications). Large — pulls in torch.
transformers
torch
