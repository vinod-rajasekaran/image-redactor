# Copy to .env and fill in. .env is gitignored and must never be committed.

# Scoring and ground-truth auditing (vision_score.py, annotate_inputs.py).
ANTHROPIC_API_KEY=sk-ant-...

# Optional, benchmark dataset downloads.
ROBOFLOW_API_KEY=...
